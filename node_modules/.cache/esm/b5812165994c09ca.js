let mongoose;_994‍.x([["default",()=>_994‍.o]]);_994‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const timeStamp = new Date()

const noteObj = {
  id: Number,
  title: String,
  body: String,
  lastEdited: {
    type: Date,
    default: timeStamp.toDa(),
  },
  private: Boolean,
}

const NotesSchema = new mongoose.Schema({
  googleId: {
    type: Number,
    required: true,
  },

  notes: {
    type: [noteObj],
    default: [],
  },
})

const Notes = mongoose.model("Notes", NotesSchema)

_994‍.d(Notes);
