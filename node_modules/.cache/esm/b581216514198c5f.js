let mongoose;_141‍.x([["default",()=>_141‍.o]]);_141‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const noteObj = {
  id: Number,
  title: String,
  body: String,
  // lastEdited: Date,
  private: Boolean,
}

const NotesSchema = new mongoose.Schema({
  googleId: {
    type: Number,
    required: true,
  },

  notes: {
    type: [noteObj],
    // default: [],
  },
})

const Notes = mongoose.model("Notes", NotesSchema)

_141‍.d(Notes);
