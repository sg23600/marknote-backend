let mongoose;_c07‍.x([["default",()=>_c07‍.o]]);_c07‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

// const timeStamp = new Date()

const noteObj = {
  id: Number,
  title: String,
  body: String,
  // lastEdited: {
  //   type: Date,
  //   default: timeStamp.toString(),
  // },
  // private: Boolean,
}

const NotesSchema = new mongoose.Schema({
  googleId: {
    type: Number,
    required: true,
  },

  notes: {
    type: [noteObj],
    default: [],
  },
})

const Notes = mongoose.model("Notes", NotesSchema)

_c07‍.d(Notes);
