let mongoose;_b3c‍.x([["default",()=>_b3c‍.o]]);_b3c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const noteObj = {
  id: Number,
  title: String,
  body: String,
  lastEdited: {
    type: Date,
    default: new Date().toLocaleString("en-US", {
      timeZone: "Asia/Kolkata",
    }),
  },
  private: Boolean,
}

const NotesSchema = new mongoose.Schema({
  googleId: {
    type: Number,
    required: true,
  },

  notes: {
    type: [noteObj],
    default: [],
  },
})

const Notes = mongoose.model("Notes", NotesSchema)

_b3c‍.d(Notes);
