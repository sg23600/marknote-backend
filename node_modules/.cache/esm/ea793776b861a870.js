let express,User;_b86‍.w("express",[["default",["express"],function(v){express=v}]]);_b86‍.w("../models/User",[["default",["User"],function(v){User=v}]]);


const route = express.Router()

route.post("/signin", async (req, res) => {
  for (const key in req.body) {
    if (req.body[key] === "") {
      return res.status(404).json("Login Error: Please fill all fields!")
    }
  }
  let userModel = new User(req.body)

  await userModel.save()
  return res.json(userModel)
})

module.exports = route
